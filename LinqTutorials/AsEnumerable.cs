using System;
using System.Linq;

namespace LinqTutorials
{
    class AsEnumerable
    {
        //static void Main(string[] args)
        //{
        //    EmployeeDBContext db = new EmployeeDBContext();

        //    var result = db.Employees
        //        .Where(d => d.Gender == "Male")
        //                        .AsEnumerable()
        //        .OrderByDescending(d => d.Salary)
        //        .Take(5)
        //        .Select(d => new Employee {
        //           FirstName = d.FirstName,
        //           Gender = d.Gender,
        //           Salary = d.Salary
        //        });

        //    foreach (Employee employee in result)
        //    {
        //        Console.WriteLine(employee.FirstName + "\t" + employee.Gender +"\t"+ employee.Salary);
        //    }
        //}
    }
}

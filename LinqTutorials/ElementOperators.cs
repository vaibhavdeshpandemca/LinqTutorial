using System;
using System.Linq;

namespace LinqTutorials
{
    class ElementOperators
    {
        //static void Main(string[] args)
        //{
        //    try
        //    {
        //        int[] numbers = { 1, 2, 3, 4, 5, 6 };
        //        //If we pass { } we get sequence contains no element.
        //        //If we use FirstOrDefault method it will take 0 as default value 
        //        //and no exception will be thrown
        //        var result = numbers.First(x => x % 2 == 0);
        //        //We also have Last and LastOrDefault method which will return last element
        //        // We also have ElementAt and ElementAtOrDefault method
        //        // Single and SingleOrDefault will return only one element if present
        //        // Or if it match the given condition.
        //        // DefaultIfEmpty will return default value if sequence contains empty or 
        //        // if it does not match condition
                
        //        Console.WriteLine("Result : {0}", result);

        //        int[] numbers2 = { 1, 2, 3, 4, 5, 6 };

        //        var result1 = numbers2.DefaultIfEmpty();

        //        foreach (var item in result1)
        //        {
        //            Console.WriteLine(item);
        //        }
        //    }
        //    catch (Exception ex)
        //    {
        //        Console.WriteLine(ex);
        //        //throw;
        //    }
        //}
    }
}
